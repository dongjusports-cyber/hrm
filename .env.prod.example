# DJ HRM — mẫu production (copy thành .env.prod trên VPS, KHÔNG commit)
APP_ENV=production
APP_NAME=DJ HRM

POSTGRES_USER=djhrm
POSTGRES_PASSWORD=doi_mat_khau_manh_o_day
POSTGRES_DB=djhrm

JWT_SECRET=doi_bang_chuoi_ngau_nhien_dai_hon_32_ky_tu
JWT_ACCESS_HOURS=8
JWT_REFRESH_DAYS=7

# Origin trình duyệt HTTPS (không dùng *)
CORS_ORIGINS=https://hrm.example.com
TRUSTED_HOSTS=hrm.example.com,localhost

GEMINI_API_KEY=
AGENT_TOKEN=doi_agent_token_manh

ADMIN_USERNAME=admin
ADMIN_PASSWORD=doi_mat_khau_admin
ADMIN_FULL_NAME=Nguyễn Thanh Thiện

# HTTP tạm (trước SSL) — chỉ 127.0.0.1; Internet vào qua Caddy 443
WEB_PUBLISH_PORT=8080
WEB_PUBLISH_BIND=127.0.0.1
LOG_DIR=/var/log/djhrm
DOCS_ENABLED=false

# P6.1 — Caddy Let's Encrypt (./ops/deploy.sh --ssl)
DOMAIN=hrm.example.com
CADDY_EMAIL=ops@example.com
