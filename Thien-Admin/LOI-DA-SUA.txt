LOI DA SUA — DJ-HRM
====================

[POL-A001] Ngưỡng chuyên cần mới (Bước A) — 2026-08-15
------------------------------------------------------
Triệu chứng:
  Ngưỡng phạt chuyên cần đang dùng số cũ GenusSuite (trễ 3 / sớm 2 / trễ 5 / sớm 4).
  Chủ + HR chốt quy định mới (§22.3, 2026-08-15): trễ >=2 hoặc sớm >=2 -> 50%;
  trễ >=5 hoặc sớm >=5 hoặc có vắng -> 0%. Trễ và sớm đếm riêng, không gộp.

Nguyên nhân:
  Tham số ngưỡng nằm ở HAI nơi và phải sửa CẢ HAI (LH-15):
    1) policy_packages.payload.attendance_penalties  -> Engine lương đọc (ẢNH HƯỞNG TIỀN)
    2) bảng attendance_bonus_rules                   -> chỉ API hiển thị
  Sửa lệch một nơi sẽ làm màn hình hiển thị mâu thuẫn với tiền thực tế.

Cách xử lý (chỉ đổi late_half 3->2 và early_zero 4->5; giữ early_half=2, late_zero=5):
  - apps/api/app/modules/policy/seed_payload.py  : late_half=2, early_zero=5
  - apps/api/app/modules/policy/seed_rates.py    : AttendanceBonusRule late_count_half=2, early_count_zero=5
  - apps/web/src/modules/config/PolicyPackagePage.tsx : default state lateHalf=2, earlyZero=5 (kèm fallback)
  - Migration dữ liệu apps/api/alembic/versions/20260815_0050_attendance_penalty_new_thresholds.py
      revision "20260815_0050", down_revision "20260812_0049"
      Postgres: jsonb_set trên payload (bọc theo dialect); dialect khác đọc-sửa-ghi bằng Python.
      Cả hai nguồn cập nhật: policy_packages (is_active) + attendance_bonus_rules (effective_to IS NULL).
      downgrade() trả về 3 và 4.

  LƯU Ý SPEC (sửa nhỏ, không đổi nghiệp vụ):
    Cột policy_packages.payload là kiểu `json` (tạo bằng sa.JSON() ở migration 0002),
    nên jsonb_set(payload,...) báo lỗi "function jsonb_set(json,...) does not exist".
    -> Ép kiểu payload::jsonb cho jsonb_set rồi ép trả lại ::json khi ghi. Ý nghĩa
       nghiệp vụ không đổi, chỉ là chi tiết kỹ thuật để câu lệnh chạy được trên cột json.

Test (đã pass):
  cd apps/api && py -3.12 -m pytest tests/test_policy_payload_v2.py tests/test_policy_rate_tables.py tests/test_attendance_penalty_44.py -q
    -> 19 passed
  Full suite API: py -3.12 -m pytest -q -> 388 passed
  Web: npm run build -> OK (tsc + vite)
  Migration Postgres (DB tạm): upgrade head (3/4 -> 2/5) -> downgrade -1 (-> 3/4) -> upgrade head (-> 2/5). OK.

An toàn:
  Phiếu lương đã phát hành gắn payslips.policy_snapshot_id -> KHÔNG bị ảnh hưởng.
  Kỳ đang mở khi tính lại sẽ dùng ngưỡng mới -> cần thông báo HR trước khi chạy lại tháng 8.

[OT-B001] Formatter OT dùng chung (Bước B) — 2026-08-15
-------------------------------------------------------
Triệu chứng:
  OT hiển thị lẫn lộn giữa các trang (LH-8): lưới ngày công hiện PHÚT thô
  (vd. "90"), trong khi lưới tháng / báo cáo hiện GIỜ. Số giờ lẻ không thống
  nhất (chỗ có hậu tố "h", chỗ không; số 0 thừa).

Nguyên nhân:
  Mỗi nơi tự định dạng riêng (cellMinutes hiện phút thô; fmtHours cục bộ trong
  OtExternalPreviewSheet; fmtNum trong Report/Overview) → không có một hàm chuẩn.

Cách xử lý (CHỈ web, KHÔNG đụng API/backend):
  - File mới apps/web/src/shared/formatOtHours.ts:
      formatOtHours(minutes, empty="") -> phút→giờ, toFixed(2), bỏ 0 thừa, hậu tố "h";
      <=0 / null / undefined -> empty. Nguồn theo GIỜ thì nhân 60 để dùng chung.
  - Test mới apps/web/src/shared/formatOtHours.test.ts theo đúng bảng spec
      (90->1.5h, 50->0.83h, 45->0.75h, 30->0.5h, 66->1.1h, 120->2h, 480->8h,
       600->10h [không thành 1h], 1200->20h, 0/null/âm -> "").
  - Thay hiển thị OT:
      * DailyGridPanel.tsx  : cột "Tăng ca sổ"/"Tăng ca ngoài" (PHÚT) -> formatOtHours(phút)
      * TimekeepingPage.tsx : lưới ngày otOnBooks/otExternal (PHÚT) -> formatOtHours(phút);
                              lưới tháng ot_hours_weekday/ot_hours_external (GIỜ) -> formatOtHours(giờ*60).
                              (Giữ cellMinutes cho TRỄ/SỚM — là phút, không phải OT.)
      * OtExternalPreviewSheet.tsx : bỏ fmtHours cục bộ, dùng chung qua fmtOt() = formatOtHours(giờ*60,"—").
      * ReportPage.tsx      : thống nhất hậu tố "h" cho ô OT (bảng theo loại + theo bộ phận),
                              giữ fmtNum (định dạng vi-VN) vì đây là tổng hợp GIỜ lớn và nằm
                              cùng dòng với reference_hours (không phải OT).
      * OverviewPage.tsx    : ĐÃ sẵn hiển thị "…h" (toLocaleng vi-VN + "h"), khớp y hệt ReportPage
                              -> đã đạt "thống nhất suffix h" (idem), không cần sửa thêm.

  QUYẾT ĐỊNH THIẾT KẾ (spec cho phép linh hoạt):
    formatOtHours nhận PHÚT (theo spec). Với các trường lưu theo GIỜ (lưới tháng,
    OT ngoài, KPI) -> nhân 60 để dùng chung một hàm. Report/Overview giữ định dạng
    vi-VN + hậu tố "h" (spec ghi "thống nhất suffix h" / "idem", KHÔNG bắt buộc formatOtHours)
    nhằm giữ dấu phân cách nghìn cho số tổng hợp lớn. LH-8 (lẫn phút/giờ) đã hết vì các
    lưới ngày nay hiển thị GIỜ.

Test (đã pass):
  cd apps/web && npm test -- formatOtHours   -> 4 test pass (gồm 600->10h)
  cd apps/web && npm run build               -> OK (tsc -b + vite, 200 modules)

Phạm vi: CHỈ Bước B. Không sửa API/backend, không đụng bước khác.

[SH-C001] Ca theo tổ vào engine + ca CLEANER (Bước C) — 2026-08-15
------------------------------------------------------------------
Triệu chứng (LH-1, LH-3, 22§22.13):
  - LH-1: resolve_work_shift_id / build_shift_cache đã chạy ở mọi call site nhưng
    chỉ GHI cột attendance_days.work_shift_id; calculate_day vẫn nhận lịch CÔNG TY,
    không tính theo ca của Tổ.
  - LH-3: ot_split hard-code mốc OT theo giờ hết ca (shift_end). Ca tạp vụ CLEANER
    hết ca 16:00 sẽ bị tính OT cho giờ nghỉ 16:00–17:00 (sai).
  - Cần: end_time (mốc hết ca / về sớm / đủ công) TÁCH khỏi ot_start (mốc OT).
    Bằng nhau ở ADMIN, khác nhau ở CLEANER (hết ca 16:00, OT từ 17:00).

Nguyên nhân:
  calculate_day + split_weekday_ot_minutes suy mốc OT từ shift_end thay vì một
  mốc OT riêng; và không có bước ánh xạ WorkShift(ca của Tổ) -> Schedule.

Cách xử lý (KHÔNG viết resolver mới — dùng day_enrich.py đã có):
  - File mới apps/api/app/modules/attendance/shift_schedule.py:
      ShiftTiming + timing_from_shift(shift, company): map WorkShift -> Schedule
      (giờ vào/trưa/hết ca theo ca; grace + work_weekdays + holiday theo lịch công ty),
      ot_start_time = shift.ot_start or shift.end_time. shift=None -> nguyên lịch công ty.
  - engine.py: calculate_day(...) + _calc_partial_workday(...) thêm kw-only
      ot_start: time|None=None. ot_qualify_after = (ot_start or afternoon_end) + grace.
      early_deadline VẪN theo afternoon_end (16:00 CLEANER) -> ra 16:00 = bình thường.
      _shift_worked_hours không đổi (đã dùng afternoon_end).
  - ot_split.py: split_weekday_ot_minutes đổi tham số shift_end -> ot_start (datetime);
      cửa sổ 17:00–20:00 tính từ ot_start, không từ shift_end (sửa LH-3). CLEANER:
      16:00–17:00 rơi ngoài cửa sổ -> 0 phút OT.
  - Nối 3 call site (lấy shift_id đã có -> timing_from_shift -> truyền schedule+ot_start):
      service.py recalculate_days (nạp shift_map 1 query, tránh N+1),
      day_grid.py (patch ô + bulk set_times), review.py (sửa tay).
      Giữ _load_schedule cho is_company_workday + danh sách ngày công.
  - seed_shifts.py: thêm CLEANER_SHIFT_CODE="CLEANER" (07:00–16:00, ot_start 17:00,
      std 8h), idempotent như ADMIN. assign_default_shift_to_teams gán tổ code "02"
      (tra theo teams.code, KHÔNG hard-code id/pk CSV 197) -> CLEANER, chỉ khi đang
      NULL/ADMIN (không đè ca HR set tay), idempotent.

Không cần migration:
  Cột work_shifts.ot_start ĐÃ CÓ sẵn (migration 20260810_0029). Spec ghi "migration
  ot_start NẾU THIẾU cột" -> cột đã tồn tại nên KHÔNG tạo migration. CLEANER là dữ liệu
  SEED (không phải migration).

Test (đã pass):
  cd apps/api && py -3.12 -m pytest tests/test_shift_cleaner.py tests/test_attendance_engine.py \
      tests/test_attendance_day_calc_33.py tests/test_work_shifts_24.py tests/test_ot_engine_45.py -q
    -> pass (thêm test_ot_split.py cũng pass, xác nhận đổi chữ ký an toàn).
  Full suite API: py -3.12 -m pytest -q -> 394 passed (388 nền + 6 test mới), không đỏ thêm.
  test_shift_cleaner.py các ca:
    CLEANER 07:00–16:00 Th2 -> early=0, worked=8, ot=0
    CLEANER 07:00–16:30      -> ot=0 (giờ nghỉ)
    CLEANER 07:00–17:30 Th3  -> ot_on_books=30 (từ 17:00)
    CLEANER 07:00–15:00      -> early=60, worked=7
    ADMIN  08:00–17:00       -> hồi quy (worked=8, ot=0)
    ADMIN  08:00–17:20 Th3   -> ot_on_books=20 (từ 17:00)
  Kiểm tra DB thật: seed CLEANER OK (07:00->16:00, ot_start 17:00, std 8.0);
    gán tổ code 02: ADMIN/NULL -> CLEANER, chạy lại idempotent (rollback, không ghi DB dev).

Phạm vi: CHỈ Bước C. Không đụng bước khác.

[WT-D001] Chế độ về sớm — bảng employee_wt_regimes (Bước D) — 2026-08-15
-----------------------------------------------------------------------
Triệu chứng:
  HR cần ghi «Thai sản / Nuôi con» trên hồ sơ NV (giờ về sớm 1–3h, có kỳ hiệu lực)
  nhưng hệ thống chỉ có status=maternity — không phân Nuôi con, không có date_from/date_to,
  không tab lọc NV đang trong chế độ.

Nguyên nhân:
  Chưa có bảng lưu chế độ theo kỳ (22§22.14). status=maternity không đủ cho Nuôi con
  và không hỗ trợ validate «không tính lùi ngày» (date_from >= today).

Cách xử lý:
  - Migration apps/api/alembic/versions/20260815_0051_employee_wt_regimes.py
      revision "20260815_0051", down_revision "20260815_0050"
  - Model EmployeeWtRegime (mdm/models.py): regime_type PREGNANT|CHILD, hours_early 1|2|3,
    date_from, date_to, ended_at, note
  - Service CRUD + validate (date_from>=today khi tạo, date_to>=date_from, không chồng kỳ),
    active_wt_regime / active_wt_regime_ids batch, gọi recalculate_days sau POST/PATCH/end
  - Router: GET/POST/PATCH/end /api/employees/{id}/wt-regimes
  - Lọc list_employees status=special_regime; cờ wt_regime_active trên EmployeeOut
  - Frontend: WtRegimePanel.tsx trong EmployeeProfileSheet (useSheetKeyboard sẵn có — không ESC trùng),
    tab «Chế độ đặc biệt» (EmployeesPage FILTER_META + HrHomePage tile → /m/hr/lists/special_regime),
    api.ts types + 4 fetch fn

Test (đã pass):
  cd apps/api && py -3.12 -m pytest tests/test_wt_regimes_api.py tests/test_employee_status_filter.py -q
    -> 6 passed
  Full suite API: py -3.12 -m pytest -q -> 398 passed (394 nền + 4 test wt mới), không đỏ thêm
  Migration Postgres: upgrade head -> downgrade -1 -> upgrade head OK
  cd apps/web && npm test -- escKeyboard -> 10 passed
  cd apps/web && npm run build -> OK (201 modules)

Phạm vi: CHỈ Bước D (data + API + UI). Engine tính giờ (Bước E) chưa làm.

[WT-E001] Engine chế độ về sớm — _apply_wt_regime (Bước E) — 2026-08-15
  - engine.py: _apply_wt_regime + kw-only wt_hours_early / standard_hours trên calculate_day
    (chỉ khi đủ vào+ra, ngày công; không bù khi thiếu punch; không áp ngày nghỉ/lễ)
  - mdm/service.py: active_wt_regime_hours_batch — 1 query prefetch (employee_ids × date_range)
  - attendance/service.py recalculate_days: truyền regime_map + timing.standard_hours vào engine
  - Test: apps/api/tests/test_wt_regime_engine.py (8 case theo KE-HOACH §Bước E)

Test (đã pass):
  cd apps/api && py -3.12 -m pytest tests/test_wt_regime_engine.py tests/test_shift_cleaner.py tests/test_attendance_engine.py -q
    -> 23 passed

Phạm vi: CHỈ Bước E (engine + prefetch recalculate_days). AI nhắc T−3 (Bước F) chưa làm.

[AI-F001] AI nhắc chế độ về sớm T−3 (Bước F) — 2026-08-15
  - ai/service.py: evaluate_wt_regime_reminders (date_to = today+3, regime còn hiệu lực)
    gọi trong list_mine cùng evaluate_payroll_reminders; source_ref wt_regime:{id}:{date_to}
  - ai/todos.py: card «N chế độ sắp hết hạn» → /m/hr/lists/special_regime
  - _visible_to: wt_regime_expiring → admin hoặc has_module("hr")
  - Test: test_wt_regime_reminders.py + test_todos_57.py (key wt_regime_expiring)

Test (đã pass):
  cd apps/api && py -3.12 -m pytest tests/test_wt_regime_reminders.py tests/test_todos_57.py -q
    -> 6 passed

[HR-G001] Cột HR phụ cấp thâm niên tiền (Bước G) — 2026-08-15
  - EmployeeOut.seniority_amount (Decimal | None) — seniority_monthly + batch tiers 1 lần/list
  - mdm/service.py: _load_seniority_rules_batch, _seniority_amount trong employee_to_out
  - EmployeesPage FULL view: cột «PC thâm niên» sau «Thâm niên»; api.ts type

Test (đã pass):
  cd apps/api && py -3.12 -m pytest tests/test_allowances_engine.py tests/test_policy_rate_tables.py tests/test_org_filter_export.py -q
    -> 21 passed
  cd apps/web && npm run build -> OK (201 modules)

[HR-H001] Cột HR phép còn lại (Bước H) — 2026-08-15
  - annual_leave_ledger.py: annual_leave_remaining_batch — load ledger + pending ALE 1 query;
    _sync_accrual_if_needed chỉ khi thiếu tháng; annual_leave_remaining gọi batch
  - EmployeeOut.annual_leave_remaining; list_employees / get_employee batch 1 lần
  - EmployeesPage FULL view: cột «Phép còn»; api.ts type

Test (đã pass):
  cd apps/api && py -3.12 -m pytest tests/test_annual_leave_ledger_47.py tests/test_nghiem_thu_47.py tests/test_payroll_ui_49.py -q
    -> 8 passed

[WK-I001] Phiếu lương mobile 3 nhóm accordion (Bước I) — 2026-08-15
  - payslip_detail.py: group_payslip_money_lines (work / allowance / deduction)
  - worker/payslips.py + WorkerPayslipDetailOut: work_lines, allowance_lines, deduction_lines
  - WorkerPayslipPage.tsx: 3 accordion «Công & nghỉ phép» / «Phụ cấp» / «Khấu trừ»
  - Không đổi số tiền — net khớp HR

Test (đã pass):
  cd apps/api && py -3.12 -m pytest tests/test_worker_payslips.py -q -> 3 passed
  cd apps/web && npm run build -> OK

[HR-J001] Tab maternity — bỏ PT/REM khỏi MATERNITY_LEAVE_CODES (J1) — 2026-08-15
  - employment_status.py: MATERNITY_LEAVE_CODES = frozenset({"MLE", "MC"})
  - PT (khám thai nửa ngày) không đẩy NV khỏi tab Chính thức; REM không thuộc 14 mã §22.6
  - Test: test_pt_leave_stays_on_active_tab, test_mle_leave_on_maternity_tab

Test (đã pass):
  cd apps/api && py -3.12 -m pytest tests/test_employee_status_filter.py -q -> 5 passed

Phạm vi: A→J1 hoàn tất trên main (2026-08-15). J2 rem_days chờ HR — chưa làm.

[HR-H002] GET /employees ghi sổ phép mỗi request rồi rollback (fix benchmark) — 2026-08-15
----------------------------------------------------------------------
Triệu chứng:
  Mở danh sách NV trên VPS ~3 giây (359 NV). Benchmark cũ báo "226 ms, đạt 300 ms".

Nguyên nhân:
  annual_leave_remaining_batch (Bước H) gọi ensure_ledger + add_entry cho từng NV trên
  GET /api/employees. get_db() không commit → 718 INSERT + 358 UPDATE bị rollback mỗi
  request, lặp vô hạn. DB production: 0 sổ phép / 0 bút toán. Đo trên cùng Session ấm
  che lỗi (226 ms); Session mới = 2.749 ms, 2.170 câu SQL.

Cách xử lý:
  - annual_leave_remaining_batch: CHỈ ĐỌC, suy ra phép còn bằng công thức (kết quả không đổi)
  - sync_accrual_batch: ghi bút toán bulk khi chốt kỳ lương (payroll/service.py)
  - test_employees_list_benchmark.py: đếm SQL trên Session mới, chặn INSERT/UPDATE trên GET
  - ops/bench_employees_vps.py: đo TTFB + số query trên VPS

Kết quả sau deploy (359 NV):
  TTFB 2.734 ms → 377 ms; SQL 2.170 → 17; 0 lệnh ghi/request.

Test (đã pass):
  cd apps/api && py -3.12 -m pytest -q -> 417 passed
  python ops/bench_employees_vps.py (VPS)

Báo cáo: Thien-Admin/BAO-CAO-BENCHMARK-API-EMPLOYEES-2026-08-15.md (bản 2, commit f02be75)

BÀI HỌC CHO AI (ghi nhớ — user yêu cầu 2026-08-15):
  1) GET chậm ~3s mà transfer ~5ms → lỗi SERVER (ghi DB lặp), KHÔNG phải JSON/mạng.
  2) Đo hiệu năng list API: Session MỚI mỗi lần + đếm SQL; ms trên Session ấm = số ảo.
  3) get_db() không commit → mọi INSERT trong GET bị rollback vô hạn (2.170 query/request).
  4) Sửa: tách read (annual_leave_remaining_batch chỉ đọc) vs write (sync_accrual_batch lúc tính lương).
  5) Hàng rào: test_employees_list_benchmark.py — assert 0 INSERT/UPDATE trên GET.
  6) Rule Cursor: .cursor/rules/hrm-perf-readonly-get.mdc

[OPS-P001] Portal khóa pass + thiếu reset_portal_passwords.sh — 2026-08-15
----------------------------------------------------------------------
Triệu chứng:
  admin / hr.demo không đăng nhập được (khóa sau 3 lần sai pass).
  Chạy MO_KHOA_PORTAL.bat / unlock_portal.py → FileNotFoundError: reset_portal_passwords.sh

Nguyên nhân:
  File ops/reset_portal_passwords.sh chưa có trong repo (unlock_portal.py upload script lên VPS).

Cách xử lý:
  - Tạo ops/reset_portal_passwords.sh: docker exec api python → unlock User, reset hash, failed_attempts=0
  - User: Thien-Admin\03-MO-KHOA-PORTAL.bat
  - AI: python ops/unlock_portal.py (cần ops/vps-root.txt)
  - Pass sau mở khóa: xem cuối unlock_portal.py hoặc DOC_BAT_DAU.txt

Ghi nhớ AI: user nói "bị khóa pass" → chạy unlock ngay, không hỏi SSH phức tạp.

[WK-I002] Phiếu lương mobile nền xanh (portal gradient lộ + header xanh dương) — 2026-08-16
----------------------------------------------------------------------
Triệu chứng:
  Công nhân mở phiếu lương trên điện thoại: nền/viền bị xanh (xanh lá portal hoặc xanh dương header).

Nguyên nhân:
  1) body portal HR có gradient xanh lá #0d8a6a (global.css) — worker-page chỉ phủ div bên trong,
     overscroll/PWA/standalone để lộ nền portal.
  2) .worker-home / .worker-payslip-detail thêm gradient xanh dương #dbeafe ở header.
  3) PWA cache-first (sw.js v2) có thể giữ CSS cũ trên ĐT sau deploy.

Cách xử lý:
  - useWorkerSurface.ts: khi vào trang worker (RequireWorker), gắn class worker-surface
    trên html/body → nền #f8fafc
  - global.css: bỏ gradient xanh header; worker-page min-height 100dvh; thêm .worker-btn-primary
  - sw.js: bump CACHE dj-hrm-worker-v3 (ĐT cần mở lại app PWA hoặc xóa cache)

Test (đã pass):
  cd apps/web && npm run build -> OK

Ghi nhớ AI: worker portal ≠ portal HR — tách nền body; đổi CSS worker thì bump sw cache.

[WK-I003] Phiếu lương mobile layout Genus (5 section, thiếu → —) — 2026-08-16
----------------------------------------------------------------------
Mục tiêu:
  B1 — giao diện gần bảng lương chi tiết GenusSuite trên điện thoại; thiếu dữ liệu hiện «—».

Cách làm (read-only — không đụng tính lương / xác nhận / khiếu nại):
  API:
  - payslip_detail.group_payslip_worker_sections: tách công / nghỉ / phụ cấp / khấu trừ
  - PayslipLineOut: label, amount, quantity, unit, target (TV = salary_divisor cho ĐVT ngày)
  - WorkerPayslipDetailOut: header (bộ phận, tổ, chức vụ, lương TV/CB — không ngày vào),
    taxable_income, subtotal mỗi section, phép năm (còn lại; hiện tại/đã dùng = null → —)
  Web:
  - WorkerPayslipPage: I–V section + bảng Mô tả/ĐVT/TV/CT/Tiền
  - workerPayslipDisplay.tsx: format — khi null
  - sw.js dj-hrm-worker-v4

Quy tắc hiển thị:
  - Field/API null → UI «—»; có số → hiện số (kể 0 đ)
  - Lương TV/CB: chỉ hiện khi > 0, else —
  - Sau khi engine/J2 bổ sung dòng → tự hiện, không sửa UI

Test (đã pass):
  cd apps/api && py -3.12 -m pytest tests/test_worker_payslips.py -q
  cd apps/web && npm run build

LOI: WK-I002 (nền xanh), WK-I001 (3 accordion).

[WK-I004] Phiếu lương mobile — template dòng Genus đủ slot (— khi trống) — 2026-08-16
----------------------------------------------------------------------
Mục tiêu:
  Luôn hiện đủ dòng cố định như Genus (công 5, nghỉ 4, phụ cấp 10, khấu trừ 7).
  Không có dữ liệu → amount/quantity null → UI «—»; có dữ liệu → hiện số.

Cách làm:
  - payslip_genus_template.py: GenusSlot + apply_genus_*_template
  - Map payslip_components theo component_code / OT note / ADJUST
  - Subtotal vẫn tính từ dòng thật (trước template), không từ —
  - sw.js dj-hrm-worker-v6

Test:
  cd apps/api && py -3.12 -m pytest tests/test_worker_payslips.py -q
  cd apps/web && npm run build

[WK-I005] Phiếu lương mobile T8 — phép năm + số ngày CT — 2026-08-17
----------------------------------------------------------------------
Triệu chứng:
  Section V chỉ có «còn lại»; «hiện tại» / «đã dùng» luôn «—».
  Cột CT làm tròn 23.04 → 23; phụ cấp T8 có tiền nhưng CT «—».

Cách xử lý (chỉ hiển thị; không tính lại T1–T7):
  - annual_leave_snapshot: định mức / used / remaining — GET read-only
  - formatWorkerQty: tối đa 2 số thập phân (vi-VN)
  - fill_missing_day_quantity khi có payslip_components (T8)
  - sw.js dj-hrm-worker-v8

Test:
  tests/test_worker_payslips.py
  tests/test_payslip_genus_template.py
  tests/test_annual_leave_ledger_47.py::test_snapshot_read_only_and_used_after_leave
  apps/web npm test -- workerPayslipDisplay

[QA-01] OT Chủ nhật / lễ vào phiếu lương (hệ số 2,0 / 3,0) — 2026-08-16
----------------------------------------------------------------------
Triệu chứng:
  Làm Chủ nhật / lễ bị nhét vào OT ngoài (ATM ×1,5), phiếu lương ot_hours_weekend /
  ot_hours_holiday = 0.

Nguyên nhân:
  engine.py ghi ot_external = toàn bộ phút ngày nghỉ. timesheet.py ưu tiên
  ot_external trước nhánh weekend/holiday.

Cách xử lý:
  - engine.py: ngày nghỉ ot_external=0, ot_on_books=0, giữ ot_minutes + ot_type
  - timesheet.py: cộng ot_hours_weekend / holiday theo ot_type trước, OT ngoài sau

Test:
  tests/test_attendance_engine.py (sunday/holiday ot_external=0)
  tests/test_timesheet.py::test_rebuild_sunday_ot_goes_to_weekend_not_external
  tests/test_attendance_days_extend_34.py (Chủ nhật ot_external_minutes=0)

[QA-02] / [REP-OV001] Tổng Quan 500 — TodoCardOut trùng — 2026-08-16
----------------------------------------------------------------------
Triệu chứng:
  GET /api/reports/overview 500 khi todo_cards không rỗng (HĐ sắp hết hạn).

Cách xử lý:
  report/schemas.py import TodoCardOut từ ai/schemas, xóa class trùng.

Test:
  tests/test_todos_57.py::test_overview_ok_when_todo_cards_nonempty

[QA-03] Worker chưa đổi mật khẩu không xem phiếu lương — 2026-08-16
----------------------------------------------------------------------
Triệu chứng:
  Mật khẩu mặc định 1234 + gọi thẳng API vẫn lấy phiếu (UI có ép đổi).

Cách xử lý:
  deps.require_worker_password_changed / WorkerPasswordOk trên
  GET payslips, GET detail, POST confirm, POST dispute.
  /me + /change-password + /login vẫn mở.

Test:
  tests/test_worker_login.py::test_worker_must_change_password_blocks_payslips
  tests/worker_auth.py — helper đổi pass cho test phiếu lương còn lại

[QA-05] AI query dispute_id cần quyền lương / khiếu nại — 2026-08-16
----------------------------------------------------------------------
Triệu chứng:
  User chỉ có ai_query đọc được số lương trong khiếu nại.

Cách xử lý:
  ai/query.py: user_can_view_disputes trước khi _dispute_context.

Test:
  tests/test_ai_query.py::test_ai_query_dispute_forbidden_without_payroll_or_dispute

[WK-PW001] Mật khẩu mặc định worker = 4 số cuối CCCD (fallback MSNV) — 2026-08-16
----------------------------------------------------------------------
Triệu chứng / yêu cầu:
  Tài khoản công nhân mới không còn mật khẩu chung 1234.

Cách xử lý:
  default_password_from_cccd: 4 số cuối CCCD; chưa có / không đủ 4 số → 4 số cuối MSNV.
  Áp dụng: seed_worker_accounts, HR unlock/reset, import GenusSuite.
  Đổi mật khẩu mới không được trùng mật khẩu mặc định của chính NV đó.

Test:
  tests/test_worker_default_password.py
  tests/test_worker_lock_security.py::test_unlock_reset_password_uses_cccd_last4
  tests/test_worker_login.py

[QA-04] Nghỉ nửa ngày = 0.5 ngày công — 2026-08-16
-------------------------------------------------
Triệu chứng:
  Đơn nghỉ nửa ngày trừ sổ 0.5 nhưng bảng công / lương cộng cứng 1 ngày.

Cách xử lý:
  Cột attendance_days.leave_days; duyệt phép ghi 0.5/1.0 theo from_half/to_half.
  aggregate_month_details + rebuild timesheet dùng leave_days (ABS 0.5, WT còn 0.5).

Test:
  tests/test_leave_requests_36.py::test_bulk_approve_half_day_leave_days
  tests/test_timesheet_month_details_35.py::test_aggregate_half_day_leave_not_full_day

[QA-06] Tính lương N+1 / chống bấm trùng — 2026-08-16
----------------------------------------------------
Triệu chứng:
  POST calculate query từng NV (phụ cấp, phép, phạt, thưởng, PIT). Bấm đúp chạy song song.

Cách xử lý:
  Prefetch batch trước vòng NV. PayrollRun status=running + HTTP 409 nếu đang tính (<15 phút).

Test:
  tests/test_payroll_calculate.py::test_calculate_rejects_when_run_already_running
  (suite calculate / benchmark vẫn pass)

[QA-07] GET Tổng Quan / bảng công không ghi kỳ lương — 2026-08-16
----------------------------------------------------------------
Triệu chứng:
  ensure_pay_period UPDATE divisor + commit mỗi lần mở trang khi status=open.

Cách xử lý:
  ensure_pay_period(..., refresh_open=False) mặc định. Chỉ True khi tính lương / rebuild.

Test:
  tests/test_timesheet.py::test_get_timesheets_does_not_refresh_open_divisor

[QA-08] OT Chủ nhật trùng ST + OT_EXT — 2026-08-16
-------------------------------------------------
Triệu chứng:
  timesheet_month_details cộng cả OT_EXT và ST/HT cho cùng giờ CN/lễ.

Cách xử lý:
  ot_type weekend/holiday → chỉ ST/HT, bỏ OT và OT_EXT.

Test:
  tests/test_timesheet_month_details_35.py::test_aggregate_sunday_ot_not_also_ot_ext

[QA-09] Ảnh hồ sơ path tuyệt đối ngoài upload — 2026-08-16
--------------------------------------------------------
Triệu chứng:
  _employee_photo_file thử Path(photo_path) tuyệt đối trước → đọc file ngoài thư mục upload.

Cách xử lý:
  Chỉ lấy file theo basename trong employee_photos (và documents/violations cùng pattern).

Test:
  tests/test_employee_photo_path.py

[QA-10] Duyệt phép không cộng lại bảng công — 2026-08-16
-------------------------------------------------------
Triệu chứng:
  bulk_decide ghi leave_code nhưng không rebuild timesheet → ngày công trên lưới cũ.

Cách xử lý:
  Sau commit duyệt, rebuild_timesheets các tháng phủ from_date–to_date (recalc_days=False).

Test:
  tests/test_leave_requests_36.py::test_bulk_approve_half_day_leave_days (đọc details sau duyệt)

[QA-11] API web không timeout — 2026-08-16
-----------------------------------------
Triệu chứng:
  apiFetch / workerFetch treo nếu API chậm.

Cách xử lý:
  AbortSignal.timeout(60_000) trên apiFetch, workerFetch, login.

[QA-12] JSON mở khóa trả new_password — 2026-08-16
-------------------------------------------------
Triệu chứng:
  UnlockResetPasswordOut.new_password lộ mật khẩu trên JSON (log proxy).

Cách xử lý:
  Bỏ field new_password. HR vẫn thấy mật khẩu trong detail (toast màn hình).

Test:
  tests/test_worker_lock_security.py

[WK-BLANK001] App công nhân màn hình trắng sau cập nhật code — 2026-08-17
---------------------------------------------------------------------------
Triệu chứng:
  Điện thoại mở PWA /worker hoặc /worker/login → màn trắng, không có form đăng nhập.

Nguyên nhân:
  sw.js cache-first lưu HTML cũ (index.html trỏ bundle JS hash cũ). Sau deploy GitHub,
  file JS mới hash khác → trình duyệt 404 → React không mount → màn trắng.

Cách xử lý:
  - sw.js v7: network-first cho navigate + /assets/*; chỉ cache icon/manifest.
  - Trên ĐT (ngay): xóa app PWA → cài lại, hoặc Chrome → Site settings → Clear data.
  - Dev .123: main.tsx đã unregister SW; mở http://IP:5173/worker/login (không localhost).

Test:
  apps/web npm run build -> OK
  Mở /worker/login trên VPS + ĐT sau khi deploy sw.js v7.

[WK-PUNCH001] Chấm công điện thoại — Main Office, nút mờ xưởng — 2026-08-17
---------------------------------------------------------------------------
Triệu chứng / yêu cầu:
  Hiện nút Chấm công cả công ty; chỉ Main Office (mã 03) bấm được; xưởng nút mờ.
  Ông Định test xong: một công tắc mở tất cả. Khóa trên server.

Cách xử lý:
  - mobile_punch_settings: mode off|allowlist|all, department_codes, extra_msnv, GPS
  - GET /api/worker/me: can_mobile_punch (không ghi DB)
  - POST /api/worker/punches: 403 ngoài allowlist; GPS khi đã đặt tọa độ; mã DJ-XXXXXX (không lưu ảnh)
  - Cấu Hình → Chấm công ĐT (admin)
  - App: nút mờ + chữ giải thích; /worker/punch chụp mặt

Test:
  apps/api py -3.12 -m pytest tests/test_worker_mobile_punch.py tests/test_worker_login.py -q
    -> 17 passed
  apps/web npm run build -> OK

[WK-PUNCH002] Chấm công ĐT — không gửi ảnh, chỉ mã DJ-XXXXXX — 2026-08-17
---------------------------------------------------------------------------
Yêu cầu:
  Điện thoại không gửi file ảnh lên VPS (nặng ~1.5MB). Chỉ gửi mã nhẹ.

Cách xử lý:
  - Máy chụp mặt local, tính SHA-256 (64 hex) gửi `photo_hash`.
  - Server trả `verify_code` dạng DJ-XXXXXX; lưu hash + mã trong `raw`, không ghi file.
  - App hiện mã to trên màn hình sau khi chấm.

Test:
  apps/api py -3.12 -m pytest tests/test_worker_mobile_punch.py -q -> 10 passed
  apps/web npm run build -> OK

[WK-PUNCH003] Chấm ĐT không hiện trên lưới — phải bấm Đồng bộ máy — 2026-08-17
---------------------------------------------------------------------------
Triệu chứng:
  App báo đã chấm (có mã DJ-XXXXXX) nhưng Chấm Công HR trống, phải bấm
  Đồng bộ / Load công từ máy vân tay mới thấy.

Nguyên nhân:
  Giờ ĐT đã ghi attendance_punches nhưng tính ngày công bị nuốt lỗi;
  bảng tháng chỉ rebuild khi Agent Mitapro chạy. ĐT không đi qua máy.

Cách xử lý:
  - Commit giờ chấm trước, rồi recalculate_days + rebuild timesheet 1 NV.
  - Lưới Chấm Công tự tải lại mỗi 20 giây (không đè khi đang sửa ô).

Test:
  tests/test_worker_mobile_punch.py::test_mobile_punch_updates_day_grid_without_mitapro
  apps/api py -3.12 -m pytest tests/test_worker_mobile_punch.py -q -> 11 passed

[AGT-TZ001] Agent .122 không tự đẩy công — phải bấm Đồng bộ khoảng ngày — 2026-08-17
--------------------------------------------------------------------------------
Triệu chứng:
  Máy vân tay đã chấm, Agent chạy 2 phút/lần, nhưng lưới HR trống.
  Chỉ hiện sau khi bấm Đồng bộ khoảng ngày (cả tháng).

Nguyên nhân:
  compute_window dùng UTC; SQL Mitapro GioCham là giờ VN naive.
  Agent lột tzinfo UTC (vd. 02:28) rồi so với GioCham 08:00 VN → bỏ hết ca sáng.

Cách xử lý:
  - to_mitapro_naive: UTC → giờ VN rồi mới gửi SQL.
  - Mỗi vòng luôn đọc từ 0h hôm nay (VN).
  - VPS tính lại công cả khi punch trùng (retry).
  - Task .122: AtLogOn + mỗi 5 phút nếu process chết.

  Trên .122: copy lại apps/agent + chạy 04-CAI-TU-CHAY-KHI-MO-MAY.bat.

Test:
  apps/agent pytest tests/test_agent_unit.py -> 8 passed
  apps/api tests/test_mitapro_ingest.py::test_duplicate_push_still_rebuilds_attendance_days

[TK-P002] Bổ sung công Enter hiện lưới nhưng tính lương không nhận — 2026-08-17
--------------------------------------------------------------------------------
Triệu chứng:
  MSNV 1519 ngày 7 và 11/08: HR Enter bổ sung giờ ra, lưới hiện công,
  bấm tính lương thì không cộng ngày đó.

Nguyên nhân:
  Máy chỉ có 1 mốc vào (07:38), worked_hours=0. HR sửa ô Ra chỉ ghi
  attendance_days (source=manual), KHÔNG ghi attendance_punches.
  Tính lương gọi rebuild_timesheets(recalc_days=True) → tính lại từ
  vân tay → nuốt giờ ra, source trả máy, ngày công = 0.

Cách xử lý:
  - patch/bulk giờ tay: ghi thêm punch source=manual.
  - recalculate_days: bỏ qua dòng source=manual (giống is_locked).
  - HR phải nhập giờ RA (vd. 17:00), chỉ giờ vào không thành ngày công.

Test:
  apps/api pytest tests/test_attendance_day_grid_37.py -q

[TK-S001] Tab Chấm công — gõ MSNV bị khựng, số NV hiện trước lưới — 2026-08-17
-------------------------------------------------------------------------------
Triệu chứng:
  Ô tìm MSNV: gõ xong phải đợi, rồi mới hiện «N nhân viên», rồi mới hiện lưới.

Nguyên nhân:
  1) Mỗi phím gõ setState trang cha → re-render AG Grid ~360 dòng (khựng khi nhập).
  2) Debounce 250ms + delay 120ms đếm NV → hai nhịp «số NV rồi lưới»
     (setDailySummary làm cha render lại, panel không memo).
  3) Lưới ngày chỉ mount sau Promise.all (timesheets + COUNT punch + leave types).
  4) AG Grid bị tháo khi «Đang tải…» rồi mount lại sau khi có data.
  5) GET ensure_pay_period gọi compute_divisor dù kỳ đã có; seed_leave_types commit mọi GET.

Cách xử lý:
  - Ô tìm giữ state local; lọc lưới qua external filter (không thay rowData 360 dòng).
  - Gõ 4 số MSNV dồn: startTransition, không debounce — debounce gom 1 lần rồi
    thay rowData = khựng ngay sau khi thả phím (gõ chậm không thấy).
  - memo DailyGridPanel; bỏ delay 120ms; overlay tải, không unmount lưới.
  - reload: setPay xong là mount lưới ngày; timesheets/status chạy song song.
  - Precompute HH:mm khi nạp dòng (tránh toLocaleTimeString lúc sort).
  - ensure_pay_period chỉ compute_divisor khi tạo kỳ / rebuild.
  - seed_leave_types chỉ commit khi thêm mã mới.

Test:
  apps/web npm test -- employeeSearch
  apps/api pytest tests/test_attendance_day_grid_37.py tests/test_timesheet.py -q

[TK-S002] AG Grid mọi tab — gõ MSNV dồn không thay rowData — 2026-08-17
------------------------------------------------------------------------
Rà soát toàn bộ AgGridReact (9 chỗ):

  Lưới ~360 NV + ô tìm MSNV (cùng lỗi Chấm công):
  - Chấm công ngày / tháng — đã [TK-S001], external filter.
  - Phép năm — rowData={filtered} mỗi phím (nhiều cột tháng).
  - Nhân sự — setQ mỗi phím re-render lưới dù chỉ Enter mới gọi API.

  Lưới ~360 NV, không ô tìm (không khựng khi gõ):
  - Bảng lương — animateRows mặc định true.
  - BH kỳ — tháo lưới khi «Đang tải…»; thiếu animateRows=false.
  - Mô phỏng lương — domLayout autoHeight tắt virtualization.

  Lưới nhỏ (không 360 NV):
  - Duyệt phép — ít dòng; animateRows=false.
  - Mitapro nhật ký / punch lệch — ≤80 / ≤120, autoHeight OK.

  Không phải AG Grid nhưng cùng UX gõ MSNV 360 người:
  - Phiếu lương (ul), Người phụ thuộc, Hợp đồng, Tăng lương.

Cách xử lý:
  - shared/ToolbarSearchInput + useAgGridExternalFilter + employeeSearch.
  - Phép năm / Nhân sự / Chấm công: lọc tại chỗ, không thay rowData.
  - Phiếu / NPT / HĐ / tăng lương: ô tìm local + startTransition.
  - Bảng lương / BH / mô phỏng / Mitapro: animateRows=false.
  - Mô phỏng: bỏ autoHeight, giữ chiều cao CSS (virtualization).
  - BH: đổi kỳ không tháo lưới (chỉ overlay «Đang cập nhật…»).

Test:
  apps/web npm test -- employeeSearch escKeyboard
  apps/web npx tsc -b

[QA-HR001] HR thao tác: duyệt phép / gán phép / tạo NV / phát hành lương âm — 2026-08-17
--------------------------------------------------------------------------------
Triệu chứng (thao tác như HR trên Cloud, 361 NV):
  1) Công nhân gửi đơn phép — Chấm công không có chỗ duyệt (panel đã viết nhưng không gắn).
  2) Gán «Nghỉ phép năm» trên lưới ngày: phiếu có dòng ALE nhưng cột phép tháng = 0.
  3) Tạo NV thiếu trường: Chrome hiện «Please fill out this field» tiếng Anh.
  4) Tính lương tháng gần như không chấm công → 361/361 phiếu thực lãnh âm;
     phát hành không cảnh báo; lưu điều chỉnh rồi phát hành luôn (thiếu Tính lại).

Cách xử lý:
  - Tab «Duyệt phép» trên Chấm công (LeaveApprovalPanel).
  - Tổng hợp tháng al_days lấy từ ABS_ALE (lưới ngày + điều chỉnh).
  - Gán phép/ghi chú trên ô: source=manual.
  - Form tạo NV noValidate — lỗi tiếng Việt từ API.
  - POST /employees/validate không còn 500 khi họ tên rỗng (Pydantic).
  - Tính lương / Phát hành: cảnh báo phiếu âm và điều chỉnh chưa tính lại.

Test:
  apps/api pytest tests/test_attendance_day_grid_37.py -q
  apps/web npx tsc -b

[QA-HR002] Quy trình kín: thai sản trên lưới tay + phiếu WD nhầm SL phép — 2026-08-17
------------------------------------------------------------------------------------
Triệu chứng (test 1 loạt: tạo NV, thôi việc, phép, thai sản/nuôi con, chấm công, tính lương, phiếu):
  1) HR chấm tay 08:00–16:00 rồi gán Thai sản 1h: ngày vẫn 7h / về sớm 60p.
     Tính lương bỏ qua source=manual nên chế độ về sớm không vào công.
  2) NV chỉ nghỉ phép, 0 ngày công: phiếu vẫn có dòng «Lương ngày công» với SL = số ngày phép.

Nguyên nhân:
  - patch_day_cell / bulk / review không truyền wt_hours_early vào calculate_day.
  - _recalc_after_regime_change gọi recalculate_days — hàm này bỏ qua source=manual.
  - _wd_parts: khi official_amount=0 vẫn emit WD với quantity=paid_days (gồm AL).

Cách xử lý:
  - Chấm/sửa giờ tay: áp hours_early của chế độ hiệu lực ngày đó.
  - Gán/sửa/chấm dứt regime: tính lại cả ngày manual từ giờ vào/ra đã lưu (giữ source).
  - Phiếu: chỉ in dòng WD khi official_amount > 0.

Xoá nhân viên: API không DELETE hồ sơ. Đường gỡ khỏi «đang làm» là Thủ tục thôi việc
(tab Thôi việc + Tái tuyển).

Test:
  apps/api pytest tests/test_hr_workflow_payroll_accuracy.py -q
  → 2 passed (công thức WD/ALE/FLE + BH + chuyên cần/đi lại + net; phiếu 3 khối).

[QA-HR003] HR thấy ô Cấu Hình trên Portal — 2026-08-17
-------------------------------------------------------
Triệu chứng: hr.demo đăng nhập vẫn thấy ô «Cấu Hình» (khóa xám). Bấm ra modal từ chối.
Cách xử lý: Portal chỉ render tab allowed; Ctrl+K ẩn Cấu Hình nếu không phải Admin.
Test: pytest tests/test_hr_uat_matrix.py::test_hr_portal_config_not_allowed

[QA-HR004] Tab BH cột nền đóng = 0; phiếu ĐT còn dòng TNCN trống — 2026-08-18
--------------------------------------------------------------------------------
Triệu chứng:
  1) Bảo Hiểm «Tổng lương tham gia BH» = 0 đ dù BHXH đang trừ (phiếu T8 ghi si_base_used, chưa có si_base_charged).
  2) Phiếu công nhân section IV vẫn có dòng «Thuế TNCN» tiền «—» (đã ẩn TNCN ở phiếu HR).

Cách xử lý:
  - Đọc si_base_charged nếu có; phiếu cũ: si_base_used khi BHXH > 0.
  - Mẫu Genus khấu trừ không còn slot PIT; phiếu ĐT lọc nhãn Thuế TNCN.

Test:
  apps/api pytest tests/test_insurance_api.py tests/test_worker_payslips.py -q

[PAY-HSE001] Bảng lương in thiếu cột HSE; OT 17:15 → 17:30 — 2026-08-18
----------------------------------------------------------------------
Triệu chứng:
  1) In Excel / phiếu Genus gộp «PCCC+HSE» — tiền HSE vẫn cộng tổng nhưng không có cột riêng.
  2) Bấm vân tay 17:00–17:30 (nghỉ cơm) bị tính OT từ 17:00 nếu ra sau 17:15.

Cách xử lý:
  - export_salary_table + payslip_genus_template: cột PCCC và HSE tách riêng.
  - ot_split: ngưỡng 17:30; phút OT vẫn từ 17:00 (ra 20:00 = 180p).
  - Vân tay 17:00–17:30 bỏ khi còn bấm sau 17:30; chỉ bấm trong khung → giờ ra, 0 OT.
  - Migration 20260818_0055 + normalize_si_policy cho gói Policy cũ.

Test:
  pytest tests/test_ot_split.py tests/test_shift_cleaner.py tests/test_attendance_engine.py
        tests/test_payslip_genus_template.py tests/test_worker_payslips.py tests/test_insurance_engine.py -q

[TK-CLEAR001] Xóa ô giờ Enter không nhận; thiếu nút xóa giờ sai — 2026-08-18
--------------------------------------------------------------------------------
Triệu chứng:
  1) Lưới ngày: bấm ô Vào/Ra, xóa giờ, Enter — giờ cũ hiện lại (saveTimeCell bỏ qua chuỗi trống).
  2) Sheet NV: ô Vào/Ra required nên không lưu trống; không có nút xóa giờ sai.

Cách xử lý:
  - Ô trống + Enter = xóa mốc đó (clear_first_in / clear_last_out); cả hai trống = clear_times.
  - Sheet: bỏ required; nút «Xóa giờ» cạnh Lưu giờ; source=manual để tính lại không khôi phục vân tay.

Test:
  pytest tests/test_attendance_day_grid_37.py tests/test_hr_workflow_payroll_accuracy.py -q
  apps/web npm test -- dailyGridTime

[PAY-G001] GET /payroll/periods/{period} INSERT kỳ lương — 2026-08-18
--------------------------------------------------------------------
Triệu chứng:
  Mở trang Tính Lương (GET) tự tạo dòng pay_periods nếu tháng chưa có.
  GET đang làm lệnh ghi — cùng lớp lỗi HR-H002 / QA-07 (đường đọc không được INSERT).

Nguyên nhân:
  publisher.get_period → ensure_pay_period → INSERT + commit khi kỳ chưa tồn tại.
  Màn Payroll còn gọi GET payslips / adjustments, hai chỗ đó cũng ensure.

Cách xử lý:
  - get_pay_period / require_pay_period: chỉ SELECT; thiếu kỳ → 404.
  - GET /payroll/periods/{period} dùng require (không ensure).
  - GET payslips + adjustments: kỳ chưa có → [] (không tạo kỳ).
  - Web: fetchPayrollPeriod 404 → null; nút Tính lương vẫn bấm được (POST mới tạo kỳ).

Test:
  pytest tests/test_payroll_period_get_readonly.py -q

[AUTH-R001] Refresh token không dùng nhưng vẫn lưu localStorage — 2026-08-18
--------------------------------------------------------------------------
Triệu chứng:
  Login cấp JWT refresh 7 ngày, SPA ghi vào djhrm_auth / djhrm_worker_auth.
  Không có POST /auth/refresh; access hết hạn thì 401 + xóa session. Token thừa
  nằm localStorage (XSS lấy được cả cặp).

Cách xử lý:
  - Không cấp refresh_token (staff + worker). Chỉ access JWT.
  - Web không lưu refreshToken; phiên cũ tự bỏ khóa khi mở lại trang.
  - Không làm endpoint refresh (chưa cần: access 8 giờ, hết hạn thì đăng nhập lại).

Test:
  pytest tests/test_auth_rbac.py tests/test_worker_login.py -q

[AL-WD001] Phép năm đếm cả Chủ nhật / lễ — 2026-08-18
----------------------------------------------------
Triệu chứng:
  Đơn ALE tính (to − from) + 1 mọi ngày lịch. Xin T6→T2 trừ 4 ngày (gồm CN).
  Thứ 7 nhà máy may là ngày công — phải trừ phép.

Cách xử lý:
  ALE chỉ trừ ngày công (work_week T2–T7, bỏ CN + holidays). Loại nghỉ khác giữ ngày lịch.
  Duyệt ALE không ghi leave_code lên Chủ nhật/lễ.

Test:
  pytest tests/test_leave_requests_36.py tests/test_calendar_divisor.py -q

[ESC-K001] ESC kẹt — không quay trang trước — 2026-08-19
------------------------------------------------------
Triệu chứng:
  Bấm ESC không thoát về trang trước (Portal / hub Nhân Sự). Giống lần kẹt
  ô tìm keep-alive, nhưng lần này còn xảy ra khi đang đứng trên trang hiện tại
  (ô tìm rỗng, ô kỳ tháng/ngày, hoặc View Transition).

Nguyên nhân (cộng dồn):
  1) Ô tìm toolbar (`data-hotkey-search`) rỗng vẫn bị `tryRevertActiveFieldEsc`
     coi là «đang sửa ô» → blur + nuốt ESC, không chạy quay trang. Bấm mãi
     cũng chỉ blur (focus có thể nhảy lại ô tìm / ô tháng).
  2) `<input type="month|date">` trên Chấm công bị coi là ô Excel → nuốt ESC.
  3) `useHrSubpageEsc` gọi `navigateSmooth` → `document.startViewTransition`.
     Transition đang chạy hoặc throw → `navigate()` không chạy → kẹt chết.
  4) Chấm công / Lương / Tổng Quan… không đăng ký tầng ESC trang, chỉ dựa
     fallback; một tầng im lặng phía trên là mất quay trang.

Cách xử lý (không gộp file ESC, không listener Escape mới):
  - Ô tìm rỗng: blur rồi `return false` → cùng một lần ESC đi tiếp stack / quay trang.
  - type date/month/week/datetime-local/color: không phải ô Excel.
  - `useHrSubpageEsc`: `navigate()` thẳng; đã đứng đúng `backTo` thì `return false`
    (hub Nhân Sự / Cấu Hình → Portal).
  - `navigateSmooth`: try/catch, fail thì vẫn `navigate`.
  - Gắn `useHrSubpageEsc` Chấm công/Lương (dưới lớp bỏ chọn NV / đóng điều chỉnh),
    HrLayout, ConfigLayout, Tổng Quan, Báo cáo, BH, Khiếu nại.

Nhớ (AI):
  Cấm startViewTransition trên đường ESC. Cấm `if (focus) return` nuốt khi không
  làm gì. Overlay không xử lý phải `return false`. Test: `cd apps/web && npm test -- escKeyboard`.
  Rule: `.cursor/rules/esc-keyboard.mdc`. LOI này.

Test:
  cd apps/web && npm test -- escKeyboard

[ATT-M001] HR sửa tay ra buổi sáng (9:10) không nhận — 2026-08-19
-----------------------------------------------------------------
Triệu chứng:
  MSNV 1519 ngày 19/08/2026: vào 07:44, ra việc nhà 09:10. HR gõ `9:10` rồi Lưu
  — hệ thống không giữ giờ ra, cột Giờ trống. Muốn ô nhập nhanh số giờ công.

Nguyên nhân:
  1) Form tháng dùng `<input type="time">` — trình duyệt từ chối `9:10` (cần `09:10`).
  2) Engine `_resolve_in_out_from_times`: mọi mốc trước 12:00 đều là VÀO → nuốt
     09:10 kể cả khi ISO hợp lệ. `apply_calc` ghi last_out = None.

Cách xử lý:
  - Lần bấm đầu tiên = vào, lần cuối = ra, bỏ hết ở giữa.
  - Cả ngày một chuỗi ≤ 60 giây (07:54–07:56) → chỉ 07:54, không lấy 07:56 làm ra.
  - Thiếu vào hoặc thiếu ra: ghi nhận mốc có, không bịa giờ / không tính công;
    AI cảnh báo, HR gọi NV lập biên bản.
  - HR chấm tay đủ vào+ra: tự tính trễ/sớm, đếm phạt chuyên cần (2 lần 50%, 5 lần 0%).
  - Form tháng: TimeInput24 + ô Giờ công + nút 4h/8h.

Test:
  cd apps/api && .venv/Scripts/python.exe -m pytest tests/test_attendance_engine.py tests/test_attendance_day_calc_33.py -q
  cd apps/web && npm test -- dailyGridTime

[ATT-P001] Lưu giờ / tích chu kỳ 2–4 giây — rebuild cả nhà máy — 2026-08-19
--------------------------------------------------------------------------
Triệu chứng:
  Bấm Lưu giờ hoặc tích/bỏ **Chu kỳ** một NV phải chờ 2–4 giây. Không phải mạng / JSON.

Nguyên nhân:
  PATCH một ngày (Lưu giờ, sửa ô, tích chu kỳ) gọi rebuild_timesheets cả kỳ
  (~359 NV, ghi TimesheetMonth + chi tiết) rồi frontend reload() cả lưới + kỳ
  + phòng ban. Bỏ tích chu kỳ không rebuild NV đó (tổng tháng lệch) nhưng
  tích chu kỳ thì quét cả nhà máy.

Cách xử lý:
  rebuild chỉ employee_id (Lưu giờ, ô lưới, tích/bỏ chu kỳ) hoặc employee_ids
  khi bulk. Không refresh divisor khi chỉ 1 NV.
  Sau lưu/chu kỳ: chỉ tải ngày của NV đó + timesheets (không reload kỳ/phòng ban).

Test:
  cd apps/api && .venv/Scripts/python.exe -m pytest tests/test_attendance_review.py -q

