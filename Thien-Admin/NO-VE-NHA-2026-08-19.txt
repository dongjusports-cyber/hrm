================================================================================
  NỢ MANG VỀ NHÀ — 19/08/2026
  GitHub đã có. Máy nhà: git pull (hoặc 04-PULL-VE-123.bat trên .123).
  KHÔNG làm trên máy .122 (Mitapro) — máy đó không Git / không sửa code.
================================================================================

Portal đang chạy:  https://hrm.dongju-v.com
Repo:              https://github.com/dongjusports-cyber/hrm   branch main
Model:             Composer 2.5

--------------------------------------------------------------------------------
1) TÍNH LẠI CÔNG (làm được ngay từ nhà, không cần Cursor)
--------------------------------------------------------------------------------

OT 8 hệ số đã deploy. File Excel mới chỉ đúng sau khi rebuild kỳ đang dùng:

  Portal → Chấm công → chọn tháng → Tính lại ngày / Tổng hợp bảng công
  → rồi mới Xuất bảng lương và Xuất OT ngoài.

Không cần máy .123. Điện thoại / máy nhà vào portal là đủ.

--------------------------------------------------------------------------------
2) GẮN dailyGridSort VÀO LƯỚI NGÀY  [WEB-SORT]
--------------------------------------------------------------------------------

Đã đẩy file, CHƯA gắn nút:

  apps/web/src/modules/timekeeping/dailyGridSort.ts
  apps/web/src/modules/timekeeping/dailyGridSort.test.ts
  DailyGridPanel.tsx — hàm sortMissingFirst() đang gọi applyDefaultSort (sai).

Việc: import applyDailyGridSort, nút «cần xử lý» gọi applyDailyGridSort(api, "needs_first").
Test:  cd apps/web && npm test -- dailyGridSort

--------------------------------------------------------------------------------
3) SCRIPT NẠP STK  [HR-STK]
--------------------------------------------------------------------------------

  apps/api/app/scripts/fill_bank_from_salary.py

Bổ sung STK / lương HĐ / ngày vào / ngày ký HĐ / SĐT từ file lương.
Chưa chạy trên VPS. Về nhà: dry-run trước, không đụng ngày sinh/CCCD.

  python -m app.scripts.fill_bank_from_salary <thu-muc-luong> --latest --dry-run
  python -m app.scripts.fill_bank_from_salary <thu-muc-luong> --latest --snapshots <snapshot> --dry-run

Ghi VPS chỉ khi user bảo deploy + chạy trên production.

--------------------------------------------------------------------------------
KHÔNG ĐẨY LÊN GITHUB (cố ý)
--------------------------------------------------------------------------------

  HIEN_PHAP ảnh / Excel lương / USB-dj-agent / ops/*passwords* / file tạm

--------------------------------------------------------------------------------
XONG VIỆC → commit push main. Deploy VPS chỉ khi user bảo.
================================================================================
