---
description: Keep-alive lưới RAM — đã GitHub, chưa deploy VPS.
alwaysApply: true
---

# Keep-alive (GitHub, chưa VPS)

**18/08/2026** commit trên `main` (máy .123 → GitHub). Portal VPS **chưa** deploy — user bảo chưa deploy.

Khi user nói **deploy VPS**: `python ops/deploy_vps_from_git.py` (không commit thêm nếu working tree bẩn).

Không gộp chế độ đặc biệt (`HrHomePage.tsx`, `mdm/service.py`, `employment_status.py`) vào lần deploy này trừ khi user yêu cầu.
